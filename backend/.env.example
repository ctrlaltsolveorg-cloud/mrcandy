# Database URL (Paste your Supabase or Neon PostgreSQL connection string here)
DATABASE_URL="postgresql://username:password@hostname:port/databasename?sslmode=require"

# JWT Secret token for signing authorization tokens
JWT_SECRET="generate_a_random_secure_secret_key_here"

# Port (Optional, defaults to 4000)
PORT=4000
